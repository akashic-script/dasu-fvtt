{{! Biography Tab }}
<section
  class='tab biography standard-form scrollable {{tab.cssClass}}'
  data-group='primary'
  data-tab='biography'
  style='height: 100%;'
>
  <fieldset style='height: 100%;'>
    <legend>{{systemFields.biography.label}}</legend>
    {{formInput @root.htmlInputField value=system.biography name="system.biography" toggled=@root.editable enriched=enrichedBiography disabled=(not @root.editable)}}
  </fieldset>
</section>